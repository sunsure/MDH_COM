%div{ style: "text-align: justify;"}
  %p= t(".dear", user_name: @user.name)

  %div{ style: "margin-left: 1em;"}
    %p= t(".intro", time: I18n.l(@user.password_reset_sent_at, format: :short))

    %p= t(".how_to_reset")

    %p= link_to t(".reset_my_password"), edit_password_reset_url(@user.password_reset_token)

  %p
    %ul{ style: "list-style-type: none; padding-left: 0em;"}
      %li= t(".my_name")
      %li= t(".admin_email")